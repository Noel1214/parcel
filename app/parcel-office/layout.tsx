import ParcelOfficeLayout from '@/components/ParcelOfficeLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ParcelOfficeLayout>{children}</ParcelOfficeLayout>;
}
